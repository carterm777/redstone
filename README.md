# Redstone Family Law — homepage demo

Static site (HTML/CSS/JS, no build step). Deployed on Vercel from `main`.

This is a pre-launch demo: `index.html` carries `noindex` and `robots.txt` blocks crawlers. Remove both when the site goes live on the firm's own domain.
